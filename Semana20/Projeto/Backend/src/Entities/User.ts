export type AuthenticationData = {
    id: string,
    login: string
}

export type UserInput = {
    login: string,
    password: string
}

export type UserComplete = {
    id: string,
    login: string,
    password: string
}